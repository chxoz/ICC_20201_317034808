Para poder compilar mi programa necesitas estar en la terminal sobre la carpeta src y poner javac main/Main.java donde main es el nombre de la carpeta y Main es la clase, como la clase Main importa un paquete al compilar el Main se compilan también las otras clases que conforman el paquete, una vez que compilamos con javac main/Main.java y no tiene errores, entonces ejecutamos con java main/Main > shapeSVG.html donde main es el nombre de la carpeta Main el nombre de la Clase que queremos ejecutar y al poner > nombredelarchivohtml.html generamos un archivo html que podemos visualizar con cualquier navegador.
